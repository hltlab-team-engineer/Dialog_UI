<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.hiroshifuu.speechapp" >

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" /> <!--Permission for GPS-->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!--Permission for GPS-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/> <!--Permission for GPS-->


    <!-- Needed only if your calling app reads numbers from the `PHONE_STATE`
         intent action. -->
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS"/>
    <uses-permission android:name="android.permission.READ_CALL_LOG"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">
        <!--<service android:name="com.example.MyConnectionService"-->
            <!--android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE">-->
            <!--<intent-filter>-->
                <!--<action android:name="android.telecom.ConnectionService" />-->
            <!--</intent-filter>-->
        <!--</service>-->
        <activity
            android:name=".activity.SpeechActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity android:theme="@style/AppTheme.NoActionBar" android:name=".activity.SpeechActivity" >-->

        <!--</activity>-->
        <!--<activity-->
            <!--android:name=".activity.MainActivity"-->
            <!--android:theme="@style/AppTheme.NoActionBar">-->
        <!--</activity>-->
    </application>
    <!--android:name=".activity.MainActivity"-->
</manifest>